# Production dependencies only (for Docker builds and runtime)
# Excludes development tools (pytest, black, isort, mypy, etc.)

# Core dependencies
click>=8.0.0
requests>=2.28.0
cryptography>=41.0.0
pygments>=2.14.0
rich>=13.0.0
pydantic>=2.0.0
